# Product Roadmap — MockHero

> Generated by PLAID. Checkboxes are updated as tasks are completed.
> The coding agent MUST mark tasks `- [x]` as they are finished.

**Status:** 39/62 tasks complete
**Current Phase:** Phase 4 — Data Generation Engine

---

## Build Philosophy

1. **Each phase ships something usable.** No phase leaves the app broken. After any phase, you can demo what you've built.
2. **Infrastructure before features.** The first phase sets up everything so feature work in later phases is fast and clean.
3. **Magic moment first.** The core value proposition — multi-table relational data generation with correct foreign keys in <1 second — works as early as possible. Everything else builds on top.
4. **Test as you go.** Don't save testing for the end. Each task includes verification.
5. **Progressive enhancement.** Start with the simplest working version, then layer on polish.
6. **Review before proceeding.** Push each completed phase as a PR and let a review agent (e.g. [CodeRabbit](https://coderabbit.ai)) review it before starting the next phase. External review catches issues the coding agent won't flag.
7. **Engine stays pure.** The data generation engine (`src/lib/engine/`) must never import Supabase, Next.js, or any I/O module. Pure TypeScript functions only — this keeps it testable, fast, and portable.
8. **API-first thinking.** The `/api/v1/generate` endpoint is the product. The dashboard and landing page are distribution for the API. Build the API layer before any UI.

---

## Phase 0: Foundation & Setup

> **Goal:** A running Next.js app deployed to Vercel with Supabase auth working (email/password + GitHub OAuth), base layout with design tokens applied, and the database schema migrated. No features yet, but the project compiles, deploys, and authenticates users.

**Reference sections — read these before starting this phase:**
- PRD: § Technical Architecture, § Data Model (Entity Definitions, Relationships, Indexes), § Design System (Color Tokens, Typography Tokens, Spacing Tokens, Component Specifications)
- Vision: § Design Direction > Design Tokens, § Design Direction > Color Palette, § Design Direction > Typography

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 0. Then read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-0/foundation-and-setup`, commit, push, and open a PR for review."

- [x] **TASK-001** — Scaffold Next.js project with TypeScript, Tailwind CSS, and App Router
  Files: `package.json`, `tsconfig.json`, `next.config.ts`, `tailwind.config.ts`, `src/app/layout.tsx`, `src/app/page.tsx`
  Notes: Run `npx create-next-app@latest . --typescript --tailwind --app --src-dir --no-import-alias`. Confirm the project uses App Router (not Pages). Add `"engines": { "node": ">=18" }` to package.json. Verify: `npm run dev` starts at localhost:3000 and renders the default page.

- [x] **TASK-002** — Install core dependencies
  Files: `package.json`
  Notes: Install production deps: `@supabase/supabase-js`, `@supabase/ssr`, `zod`, `clsx`, `tailwind-merge`. Install dev deps: `vitest`, `@testing-library/react`, `@testing-library/jest-dom`. Do NOT install Polar SDK yet (Phase 5). Verify: `npm run build` succeeds with no errors.

- [x] **TASK-003** — Configure Tailwind with MockHero design tokens
  Files: `tailwind.config.ts`, `src/app/globals.css`
  Notes: Extend Tailwind theme with design tokens from docs/prd.md § Design System. Map CSS custom properties: `--color-primary: #6C5CE7`, `--color-primary-hover: #5A4BD5`, etc. Add font families: Inter (heading + body) and JetBrains Mono (mono). Add all spacing, border-radius, and shadow tokens. Include dark mode tokens under `[data-theme="dark"]`. Set content paths to `./src/**/*.{ts,tsx}`. Import Inter and JetBrains Mono via `next/font/google` in the root layout (not via CSS `@import`). Verify: Create a test div with `bg-primary text-white` class — it renders purple (#6C5CE7) background.

- [x] **TASK-004** — Set up environment variables and Supabase project configuration
  Files: `.env.local`, `.env.example`, `.gitignore`
  Notes: Create `.env.local` with placeholders for `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `NEXT_PUBLIC_APP_URL` (set to `http://localhost:3000` for dev). Create `.env.example` with the same keys but empty values. Ensure `.env.local` is in `.gitignore`. Add Polar placeholders: `POLAR_API_KEY`, `POLAR_WEBHOOK_SECRET`, `POLAR_ORGANIZATION_ID` (leave empty for now). Verify: `process.env.NEXT_PUBLIC_SUPABASE_URL` is accessible in a server component.

- [x] **TASK-005** — Create Supabase client utilities (browser, server, admin)
  Files: `src/lib/supabase/client.ts`, `src/lib/supabase/server.ts`, `src/lib/supabase/admin.ts`
  Notes: `client.ts` exports `createBrowserClient()` using `@supabase/ssr` `createBrowserClient` for client components. `server.ts` exports `createServerClient()` using `@supabase/ssr` `createServerClient` with cookie helpers for server components and route handlers. `admin.ts` exports `createAdminClient()` using `@supabase/supabase-js` `createClient` with the service role key — this bypasses RLS and must only be used in API route handlers. Per the PRD § Technical Architecture > Common Gotchas, the anon key is safe for client-side; the service_role key must never reach the browser. Verify: Import `createServerClient` in `src/app/page.tsx` and log the Supabase URL to confirm it connects.

- [x] **TASK-006** — Create Supabase database migration with full schema
  Files: `supabase/migrations/001_initial_schema.sql`
  Notes: Copy the entire SQL from docs/prd.md § Data Model > Entity Definitions, including the `profiles`, `api_keys`, `usage_logs`, `daily_usage`, and `subscriptions` tables. Include all RLS policies, indexes from § Data Model > Indexes, functions (`handle_new_user`, `update_updated_at`), and triggers. Enable `pgcrypto` extension. This migration should be run manually via Supabase SQL editor or `supabase db push` once the Supabase project is created. Verify: All 5 tables exist in Supabase with correct columns. The `handle_new_user` trigger fires on auth.users insert.

- [x] **TASK-007** — Implement Next.js middleware for Supabase session refresh
  Files: `src/middleware.ts`
  Notes: Create middleware that refreshes the Supabase auth session on every request using `@supabase/ssr` `createServerClient` with the `request` and `response` cookie helpers. Match all routes except static assets and API routes that use API key auth (match `/((?!_next/static|_next/image|favicon.ico|api/v1/).*)` ). Per PRD § Security Considerations > Authentication Flow, sessions are stored in HTTP-only cookies via `@supabase/ssr`. Verify: After signing in, refreshing any page keeps the session alive.

- [x] **TASK-008** — Build base layout with root layout, fonts, and metadata
  Files: `src/app/layout.tsx`, `src/app/globals.css`
  Notes: Root layout loads Inter and JetBrains Mono via `next/font/google`. Sets `<html lang="en">` and applies font variables to the body. Metadata: title "MockHero — Realistic Test Data API", description "Send a schema, get back realistic, relational fake data that looks real." OG image placeholder at `/og-image.png`. Add a basic favicon. Apply base styles: `bg-bg text-text` using the CSS custom properties. Verify: View source shows correct meta tags. Fonts load correctly.

- [x] **TASK-009** — Build utility functions (cn helper, constants)
  Files: `src/lib/utils/cn.ts`, `src/lib/utils/constants.ts`
  Notes: `cn.ts` exports a `cn()` function using `clsx` + `tailwind-merge` for conditional class merging. `constants.ts` exports tier limits as a typed object: `TIER_LIMITS = { free: { dailyRecords: 1000, perRequest: 100, perMinute: 10 }, pro: { dailyRecords: 100000, perRequest: 10000, perMinute: 60 }, scale: { dailyRecords: 1000000, perRequest: 50000, perMinute: 120 } }`. Also export `API_KEY_PREFIX = "mh_"`. Per PRD § Functional Requirements > FR-015. Verify: Import and use `cn('bg-red', false && 'hidden')` — returns `'bg-red'`.

- [x] **TASK-010** — Implement auth pages (login, signup, OAuth callback)
  Files: `src/app/(auth)/login/page.tsx`, `src/app/(auth)/signup/page.tsx`, `src/app/(auth)/callback/route.ts`
  Notes: Login page: centered card with email/password form and "Sign in with GitHub" button. Signup page: same layout with "Sign up with GitHub" button. Both use Supabase Auth `signInWithPassword`, `signUp`, and `signInWithOAuth({ provider: 'github' })`. Callback route at `/callback` handles the OAuth code exchange using `@supabase/ssr`. On success, redirect to `/dashboard`. On error, redirect to `/login?error=auth`. Style per PRD § UI/UX Requirements > Screen: Login and Screen: Signup. Use design tokens. Verify: Sign up with email, sign in with email, sign in with GitHub — all redirect to /dashboard. Invalid credentials show inline error.

- [x] **TASK-011** — Create dashboard layout with navigation shell
  Files: `src/app/dashboard/layout.tsx`, `src/app/dashboard/page.tsx`
  Notes: Dashboard layout: top navigation bar with MockHero logo (text for now), nav links (Dashboard, Settings), and user avatar/name with a dropdown (sign out). Protect this route — redirect to `/login` if no Supabase session. Dashboard page: placeholder content "Dashboard — coming soon" with the user's email displayed. Per PRD § UI/UX Requirements > Screen: Dashboard. Verify: Unauthenticated users are redirected to /login. Authenticated users see the dashboard shell with their email.

---

## Phase 1: Data Generation Engine

> **Goal:** The core data generation engine works as a standalone TypeScript module. Given a single-table schema, it generates realistic, locale-aware, seeded data with all 30+ field types. The engine is pure TypeScript with no external I/O — tested via Vitest, not via HTTP.

**Reference sections — read these before starting this phase:**
- PRD: § API Specification > POST /api/v1/generate (Request Parameters, field types), § Functional Requirements > FR-001 through FR-007, FR-010, § API Specification > GET /api/v1/types
- Vision: § Product Strategy > MVP Definition

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 1. Then read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-1/data-generation-engine`, commit, push, and open a PR for review."

- [x] **TASK-012** — Implement seeded PRNG (mulberry32)
  Files: `src/lib/engine/prng.ts`
  Notes: Implement the mulberry32 PRNG algorithm as a pure function. Export a `createPRNG(seed: number)` function that returns an object with `next(): number` (0–1 range), `nextInt(min, max): number`, `nextFloat(min, max, precision): number`, `pick<T>(array: T[]): T`, and `weightedPick<T>(items: T[], weights: number[]): T`. The `weightedPick` function handles normalized and unnormalized weights. If no seed is provided, generate a random seed using `Math.random() * 2**32`. Per FR-006 — all randomness must flow through this PRNG; no `Math.random()` anywhere in the engine. Verify: `createPRNG(42).next()` returns the same value every time. Two PRNGs with different seeds return different sequences.

- [x] **TASK-013** — Define TypeScript types for schema, config, and output
  Files: `src/lib/engine/types.ts`
  Notes: Define and export: `FieldType` (union of all 30+ field type strings), `FieldDefinition` (name, type, params?, nullable?), `TableDefinition` (name, count, fields), `GenerateRequest` (tables, locale?, format?, seed?, sql_dialect?), `GenerateResult` (data: Record<string, Record<string, unknown>[]>, meta: GenerateMeta), `GenerateMeta` (tables, total_records, records_per_table, locale, format, seed, generation_time_ms), `Locale` ('en' | 'de' | 'fr' | 'es' | 'hr'), `OutputFormat` ('json' | 'csv' | 'sql'), `SqlDialect` ('postgres' | 'mysql' | 'sqlite'). Also define `GeneratorContext` (prng, locale, tableData for ref resolution). Per PRD § API Specification. Verify: Types compile with `tsc --noEmit`.

- [x] **TASK-014** — Build schema parser and validator with Zod
  Files: `src/lib/engine/schema-parser.ts`
  Notes: Create a Zod schema matching the `GenerateRequest` type. Validate: all field types are in the recognized set (from types.ts), required params are present per field type (e.g., `enum` requires `values`, `ref` requires `table` + `field`), table names are unique, field names within each table are unique, table counts are positive integers, ref fields point to tables that exist in the request. Return structured errors with field paths per PRD § API Specification > Error Response Format. Implement typo suggestions for unknown field types (Levenshtein distance). Export `parseSchema(input: unknown): { success: true, data: GenerateRequest } | { success: false, errors: ValidationError[] }`. Per FR-001. Verify: Valid schema parses correctly. Invalid schema returns errors with correct field paths. Unknown type "strig" suggests "string".

- [x] **TASK-015** — Build locale data for English (en) locale
  Files: `src/lib/engine/locale-data/en.ts`
  Notes: Create data file with: 100+ weighted first names (mix of common American/British names with frequency weights), 100+ weighted last names, 50+ real US/UK cities with corresponding postal codes, phone number format `+1 (XXX) XXX-XXXX`, email domain patterns (`gmail.com`, `outlook.com`, `yahoo.com`, `icloud.com` with weights), street name patterns. Names should be weighted so common names (James, Mary, Smith, Johnson) appear more frequently. Export as a typed `LocaleData` object. Per FR-004. Verify: Import the locale data and confirm the structure matches the `LocaleData` type.

- [x] **TASK-016** — Build locale data for German (de), French (fr), Spanish (es), and Croatian (hr)
  Files: `src/lib/engine/locale-data/de.ts`, `src/lib/engine/locale-data/fr.ts`, `src/lib/engine/locale-data/es.ts`, `src/lib/engine/locale-data/hr.ts`
  Notes: Each file follows the same `LocaleData` structure as en.ts. De: 100+ German first/last names (Maximilian, Bergmann, etc.), German cities with PLZ codes, phone format `+49 XXX XXXXXXXX`, domains `.de`. Fr: French names, cities with codes postaux, `+33 X XX XX XX XX`, `.fr`. Es: Spanish names, cities with postal codes, `+34 XXX XXX XXX`, `.es`. Hr: Croatian names, cities with postal codes, `+385 XX XXX XXXX`, `.hr`. Per FR-004 — locale "de" must produce German names, cities, phone numbers, and email domains. Verify: Each locale data file exports a valid `LocaleData` object with >100 first names and >100 last names.

- [x] **TASK-017** — Implement identity field generators (first_name, last_name, full_name, email, username, phone)
  Files: `src/lib/engine/field-generators/identity.ts`
  Notes: Each generator is a pure function taking `(prng, params, context: GeneratorContext)`. `first_name`: weighted random pick from locale data. `last_name`: weighted random pick. `full_name`: combines first + last. `email`: derives from first_name + last_name in the same record (if available in context) with locale-appropriate domain — e.g., `maximilian.bergmann@outlook.de`. Format variations: `first.last@`, `firstlast@`, `first.last42@`. `username`: derived from name or random pattern like `mbergmann42`. `phone`: locale-formatted per locale data patterns. Per FR-003. Verify: Generate 10 identity records with locale "de" — all names are German, emails use `.de` domains, phone numbers start with `+49`.

- [x] **TASK-018** — Implement location, business, temporal, and technical field generators
  Files: `src/lib/engine/field-generators/location.ts`, `src/lib/engine/field-generators/business.ts`, `src/lib/engine/field-generators/temporal.ts`, `src/lib/engine/field-generators/technical.ts`
  Notes: Location: `city` (from locale), `country`, `postal_code` (paired with city from locale), `address` (street + number + postal + city, locale-formatted). Business: `company_name` (realistic patterns), `product_name` (with optional category param), `rating` (min/max/precision params), `price` (min/max/precision). Temporal: `datetime` (ISO 8601, min/max params), `date` (ISO 8601 date only, min/max). Technical: `uuid` (v4 format from PRNG, not crypto), `id` (auto-incrementing with start param), `url`, `ip_address` (v4/v6 param), `color_hex`, `json` (from mini-schema param). All generators use the seeded PRNG — no `Math.random()`. Per FR-003. Verify: Generate records with each field type and confirm output matches expected format.

- [x] **TASK-019** — Implement content and logic field generators
  Files: `src/lib/engine/field-generators/content.ts`, `src/lib/engine/field-generators/logic.ts`
  Notes: Content: `sentence` (min_words/max_words params, realistic word selection), `paragraph` (min/max sentences), `title` (title-case short text), `slug` (URL-friendly from title), `image_url` (picsum.photos with dimensions params). Logic: `boolean` (probability param), `enum` (values + optional weights arrays), `integer` (min/max), `decimal` (min/max/precision), `ref` (placeholder — actual resolution in TASK-023), `sequence` (start/step/prefix/suffix), `constant` (fixed value). Handle `nullable` flag for all generators — when `nullable: true`, use `params.null_rate` (default 0.15) to decide if value is null. Per FR-003, FR-010. Verify: `enum` with weights `[5, 80, 15]` skews distribution correctly. `sequence` with prefix "ORD-" and start 1001 produces "ORD-1001", "ORD-1002", etc.

- [x] **TASK-020** — Build the main generator function for single-table generation
  Files: `src/lib/engine/generator.ts`
  Notes: Export `generate(request: GenerateRequest): GenerateResult`. For this task, handle single-table schemas only (multi-table with refs comes in Phase 2). Flow: validate schema with `parseSchema`, create PRNG from seed (or random), load locale data, iterate through each table and generate `count` records by calling the appropriate field generator for each field. Track `generation_time_ms` using `performance.now()`. Build the `meta` object. Return `{ data, meta }`. The generator must be a pure function with no I/O. Per FR-007. Verify: `generate({ tables: [{ name: "users", count: 5, fields: [{ name: "id", type: "uuid" }, { name: "first_name", type: "first_name" }] }], locale: "de", seed: 42 })` returns 5 records with German first names and the same output every time.

- [x] **TASK-021** — Write Vitest unit tests for the engine
  Files: `src/lib/engine/__tests__/prng.test.ts`, `src/lib/engine/__tests__/generator.test.ts`, `src/lib/engine/__tests__/schema-parser.test.ts`
  Notes: Test PRNG: determinism (same seed = same sequence), distribution (values are between 0 and 1), weighted pick accuracy. Test schema parser: valid schema passes, unknown field type returns error with suggestion, ref to nonexistent table returns error, duplicate table names error, missing required params error. Test generator: single-table generation returns correct count, locale "de" produces German names, seed determinism (same seed = same output), all 30+ field types produce valid output. Per Build Philosophy item 4. Verify: `npx vitest run` — all tests pass.

---

## Phase 2: Relational Data & API

> **Goal:** Multi-table relational data generation with foreign key resolution (the magic moment). The `/api/v1/generate` endpoint is live, authenticated with API keys, and returns JSON, CSV, and SQL output. A developer can call the API with a multi-table schema and get back relationally-correct data in <1 second.

**Reference sections — read these before starting this phase:**
- PRD: § Functional Requirements > FR-002, FR-005, FR-008, FR-009, FR-013, FR-014, FR-015, § API Specification > POST /api/v1/generate, GET /api/v1/types, GET /api/v1/health, § Security Considerations > API Key Security, Rate Limiting
- Vision: § Product Strategy > Magic Moment

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 2. Then read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-2/relational-data-and-api`, commit, push, and open a PR for review."

- [x] **TASK-022** — Implement topological sort for table dependency resolution
  Files: `src/lib/engine/topological-sort.ts`
  Notes: Export `topologicalSort(tables: TableDefinition[]): TableDefinition[] | { cycle: string[] }`. Build a directed graph from ref fields: if `orders.user_id` refs `users.id`, add edge `orders → users` (orders depends on users). Perform Kahn's algorithm or DFS-based topological sort. If a cycle is detected, return the cycle path as an array of table names (e.g., `["A", "B", "C", "A"]`). Per FR-002. Verify: `[users, orders(refs users), reviews(refs users, products)]` sorts to `[users, products, orders, reviews]` (or any valid topological order). Circular `[A refs B, B refs A]` returns the cycle.

- [x] **TASK-023** — Implement ref field resolution with uniform and power_law distribution
  Files: `src/lib/engine/field-generators/logic.ts` (update ref generator), `src/lib/engine/generator.ts` (update to pass tableData context)
  Notes: Update the `ref` generator to look up already-generated data for the referenced table and field from `GeneratorContext.tableData`. `uniform` distribution: each parent record has equal probability. `power_law` distribution: implement Pareto-like distribution where ~20% of parent records get ~80% of references — use the Zipf distribution via `1 / rank^alpha` with alpha=1.0. Update `generator.ts` to sort tables via topological sort before generation, and pass previously generated table data into the context for each subsequent table. Per FR-005. Verify: Generate users (50) + orders (200) with `orders.user_id` ref to `users.id`. Every `user_id` in orders exists in users. With `power_law`, some users have many orders, most have few.

- [x] **TASK-024** — Build output formatters (JSON, CSV, SQL)
  Files: `src/lib/engine/formatters/json-formatter.ts`, `src/lib/engine/formatters/csv-formatter.ts`, `src/lib/engine/formatters/sql-formatter.ts`
  Notes: JSON formatter: already the default output — just wraps data + meta into the response shape. CSV formatter: per FR-008, one CSV string per table with headers. Escape commas, quotes, newlines per RFC 4180. SQL formatter: per FR-009, support three dialects. Postgres: `UUID`, `TIMESTAMPTZ`, `SERIAL`, multi-row INSERT. MySQL: `CHAR(36)`, `DATETIME`, `AUTO_INCREMENT`, multi-row INSERT. SQLite: `TEXT` for all types, individual INSERT statements. CREATE TABLE before INSERTs. Tables ordered by dependency (parents first). Export a `formatOutput(data, meta, format, sqlDialect?)` function. Verify: Generate a 2-table schema, format as SQL (postgres dialect) — output contains valid CREATE TABLE and INSERT statements with correct FK references.

- [x] **TASK-025** — Build API error handling utilities
  Files: `src/lib/api/errors.ts`
  Notes: Export standardized error response helpers: `validationError(message, details[])`, `unauthorizedError(message)`, `rateLimitError(message, details[])`, `internalError(message)`, `schemaError(message, details[])`, `dependencyCycleError(cyclePath)`. Each returns a `NextResponse` with the correct status code and the error shape from PRD § API Specification > Error Response Format: `{ error: { code, message, details? } }`. Error codes: `VALIDATION_ERROR` (400), `UNAUTHORIZED` (401), `RATE_LIMIT_EXCEEDED` (429), `INTERNAL_ERROR` (500), `SCHEMA_ERROR` (400), `DEPENDENCY_CYCLE` (400). Verify: `validationError("Invalid schema", [{ field: "tables[0].fields[0].type", message: "Unknown type" }])` returns a Response with status 400 and correct JSON body.

- [x] **TASK-026** — Build API key validation middleware
  Files: `src/lib/api/middleware.ts`
  Notes: Export `validateApiKey(request: NextRequest): Promise<{ user_id: string, tier: 'free' | 'pro' | 'scale' } | NextResponse>`. Extract `x-api-key` header, hash with SHA-256 (use Web Crypto API's `crypto.subtle.digest`), look up in `api_keys` table where `is_active = true` using the admin Supabase client. If found, resolve the user's tier from the `profiles` table (check for active subscription in `subscriptions` table to determine if pro/scale). Return the user info or a 401 error response. Per FR-013 — validation must be <50ms (indexed lookup). Verify: Create a test API key in Supabase, call the middleware with it — returns user_id and tier. Invalid key returns 401.

- [x] **TASK-027** — Build rate limiting logic
  Files: `src/lib/api/rate-limiter.ts`
  Notes: Export `checkRateLimit(userId: string, tier: string, requestedRecords: number): Promise<{ allowed: true, remaining: number, limit: number, reset: string } | { allowed: false, error: NextResponse }>`. Check per-request record limit from `TIER_LIMITS` in constants.ts. Check daily record limit by querying `daily_usage` for today's date. Per-minute rate limiting: use an in-memory `Map<string, { count: number, windowStart: number }>` sliding window — acceptable to reset on cold start per PRD. Return rate limit headers object: `X-RateLimit-Limit`, `X-RateLimit-Remaining`, `X-RateLimit-Reset`, `X-RateLimit-Records-Limit`, `X-RateLimit-Records-Remaining`. Per FR-015. Verify: A free-tier user requesting 101 records gets a 429. A free-tier user with 999 daily records used requesting 50 more gets a 429.

- [x] **TASK-028** — Implement POST /api/v1/generate route handler
  Files: `src/app/api/v1/generate/route.ts`
  Notes: POST handler: validate API key (TASK-026), check rate limit (TASK-027), parse request body with Zod schema, call `generate()` from the engine, format output, log usage to `usage_logs` and upsert `daily_usage` (fire-and-forget — don't block response), return formatted response with rate limit headers. Handle all error cases: 400 for validation errors, 401 for auth, 429 for rate limits, 500 for unexpected errors. Per FR-014 — usage logging happens after sending response. Set Vercel function timeout to 30s in `next.config.ts` for large generation requests. Verify: `curl -X POST http://localhost:3000/api/v1/generate -H "x-api-key: <your-key>" -H "Content-Type: application/json" -d '{"tables":[{"name":"users","count":5,"fields":[{"name":"id","type":"uuid"},{"name":"name","type":"full_name"}]}]}'` returns 5 users with realistic names.

- [x] **TASK-029** — Implement GET /api/v1/types route handler
  Files: `src/app/api/v1/types/route.ts`
  Notes: Public endpoint (no auth required). Return the complete field types catalog from PRD § API Specification > GET /api/v1/types — categorized by identity, location, business, temporal, technical, content, logic. Each type includes: type name, description, params object, and example value. Also include `locales`, `formats`, and `sql_dialects` arrays. This is the agent discovery endpoint — AI agents call this to understand available types. Per FR-003. Verify: `curl http://localhost:3000/api/v1/types` returns the full types catalog. The response is valid JSON with all 30+ types.

- [x] **TASK-030** — Implement GET /api/v1/health route handler
  Files: `src/app/api/v1/health/route.ts`
  Notes: Public endpoint. Return `{ status: "ok", version: "1.0.0", timestamp: <ISO string> }`. Optionally check Supabase connectivity — if unreachable, return `{ status: "degraded", version: "1.0.0", timestamp: ... }` with a 200 status (still reachable, but DB is down). Per PRD § Non-Functional Requirements > Reliability. Verify: `curl http://localhost:3000/api/v1/health` returns `{"status":"ok","version":"1.0.0","timestamp":"..."}`.

- [x] **TASK-031** — Write integration tests for the API endpoints
  Files: `src/app/api/v1/__tests__/generate.test.ts`, `src/app/api/v1/__tests__/types.test.ts`
  Notes: Test /generate: valid single-table request returns 200 with correct data shape, valid multi-table with refs returns relationally-correct data, invalid schema returns 400, missing API key returns 401, exceeding per-request limit returns 429. Test /types: returns 200 with all field type categories. Test determinism: same seed + same schema = identical response. Test the magic moment: 4-table schema (users, products, orders, reviews) with refs generates correctly with all foreign keys valid. Verify: `npx vitest run` — all integration tests pass.

---

## Phase 3: Dashboard & Usage Tracking

> **Goal:** A functional developer dashboard where authenticated users can see their API key, copy it, regenerate it, view usage statistics (daily progress bar, 7-day chart), and see their current tier with an upgrade path. API key generation happens automatically on signup.

**Reference sections — read these before starting this phase:**
- PRD: § Functional Requirements > FR-011, FR-012, FR-018, FR-019, § UI/UX Requirements > Screen: Dashboard, Screen: Settings, § Design System > Component Specifications
- Vision: § Design Direction > Component Style Guide

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 3. Then read only the Reference sections listed above from docs/prd.md and docs/product-vision.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-3/dashboard-and-usage-tracking`, commit, push, and open a PR for review."

- [x] **TASK-032** — Build reusable UI components (Button, Input, Card, Badge, Modal, Toast)
  Files: `src/components/ui/button.tsx`, `src/components/ui/input.tsx`, `src/components/ui/card.tsx`, `src/components/ui/badge.tsx`, `src/components/ui/modal.tsx`, `src/components/ui/toast.tsx`, `src/components/ui/skeleton.tsx`
  Notes: Build per PRD § Design System > Component Specifications. Button: primary/secondary/ghost variants, sm/md/lg sizes, disabled state (opacity 0.5), loading state with inline spinner. Input: 40px height, border with focus ring `ring-2 ring-primary/20`. Card: surface-elevated bg, border, rounded-lg, shadow-sm, padding `var(--space-6)`. Badge: sm rounded-full with color variants (default, success, warning, error). Modal: overlay with centered card, close button, body, footer with action buttons. Toast: fixed bottom-right, auto-dismiss after 3s, success/error variants, uses `aria-live="polite"`. Skeleton: pulsing placeholder with rounded shape. Use the `cn()` utility for all class merging. Verify: Render each component in isolation — styles match design tokens.

- [x] **TASK-033** — Implement API key generation on user signup
  Files: `src/lib/api/keys.ts`
  Notes: Export `generateApiKey(userId: string): Promise<{ rawKey: string, keyPrefix: string }>`. Generate a raw key as `mh_` + 32-byte random hex (64 hex characters). Compute SHA-256 hash of the full key. Store in `api_keys` table: `user_id`, `key_hash`, `key_prefix` (first 12 chars, e.g., `mh_a1b2c3d4`), `is_active: true`. Use the admin Supabase client. Also export `regenerateApiKey(userId: string)`: revoke old key (set `is_active: false`, set `revoked_at`), then generate new. Export `getActiveKeyPrefix(userId: string)`: return the prefix of the active key. Per FR-012. Verify: Call `generateApiKey` — returns a key starting with `mh_`. The hash stored in DB matches the SHA-256 of the raw key.

- [x] **TASK-034** — Auto-generate API key on first dashboard visit
  Files: `src/app/dashboard/page.tsx`
  Notes: On the dashboard page server component, check if the user has an active API key. If not (first visit after signup), auto-generate one and store the raw key in a server-side cookie or pass it to the client component as a one-time prop. This ensures the raw key is shown exactly once. Subsequent visits only show the prefix. Use a query param `?new_key=true` or similar mechanism to signal the client to display the full key with the warning "This key will only be shown once. Copy it now." Per FR-012 and US-006. Verify: Sign up a new user, visit /dashboard — API key is displayed in full with a copy button. Refresh — only the prefix is shown.

- [x] **TASK-035** — Build API Key Card dashboard component
  Files: `src/components/dashboard/api-key-card.tsx`
  Notes: Client component. Shows the API key prefix with a masked remainder (e.g., `mh_a1b2c3d4••••••••`). "Copy" button copies the key prefix (or full key on first view). "Regenerate" button opens a confirmation modal warning that the old key will stop working. Includes a quick-start cURL example pre-filled with the user's key prefix: `curl -X POST https://mockhero.dev/api/v1/generate -H "x-api-key: mh_a1b2c3d4..." -H "Content-Type: application/json" -d '{"tables":[...]}'`. Use the CodeBlock component for the cURL example. Per FR-018 and US-006, US-007. Verify: API key card renders with prefix, copy button copies to clipboard and shows toast, regenerate shows modal.

- [x] **TASK-036** — Build Usage Stats dashboard component with progress bar and 7-day chart
  Files: `src/components/dashboard/usage-chart.tsx`, `src/components/dashboard/tier-info.tsx`
  Notes: Usage chart component: fetch `daily_usage` for the last 7 days. Show today's usage as a progress bar (records used / daily limit). Progress bar color: green (0–74%), amber (75–89%), red (90–100%). Below the progress bar, render a simple bar chart of the last 7 days using SVG (no external charting library per FR-019). Tier info component: show current tier as a badge (Free/Pro/Scale), records limit, and "Upgrade to Pro" CTA button for free-tier users. Per FR-019 and US-008. Verify: Dashboard shows progress bar at correct percentage, 7-day chart renders bars for each day, tier badge displays correctly.

- [x] **TASK-037** — Wire up the full dashboard page
  Files: `src/app/dashboard/page.tsx`
  Notes: Combine the API Key Card (TASK-035), Usage Chart (TASK-036), and Tier Info (TASK-036) into the dashboard page. Layout: API Key Card at the top (full width), Usage Stats section below with progress bar and 7-day chart side by side on desktop (stacked on mobile). First-visit state: show full API key with welcome message "Your API key is ready. Try this cURL command to generate your first data." Error state: if Supabase query fails, show "Couldn't load usage data. Refresh to try again." Loading state: skeletons for usage chart and stats. Per PRD § UI/UX Requirements > Screen: Dashboard. Verify: Dashboard renders all sections. New user sees full key and welcome message. Returning user sees prefix and usage stats.

- [x] **TASK-038** — Build settings page
  Files: `src/app/dashboard/settings/page.tsx`
  Notes: Nested under dashboard layout. Three sections: Account Info (display name, email — read-only), Subscription (current tier, status, "Manage Subscription" button — disabled until Polar is integrated in Phase 5), Danger Zone ("Delete Account" button with confirmation modal — implement the delete using Supabase admin client to delete the user from auth.users, which cascades to all related tables). Per PRD § UI/UX Requirements > Screen: Settings. Verify: Settings page renders account info correctly. Delete account button shows confirmation modal. Confirming delete removes the user.

- [x] **TASK-039** — Add auth hooks for client components
  Files: `src/hooks/use-auth.ts`, `src/hooks/use-api-key.ts`
  Notes: `use-auth.ts`: custom hook using `@supabase/ssr` browser client to provide `user`, `session`, `loading`, and `signOut()`. Subscribes to `onAuthStateChange`. `use-api-key.ts`: hook to fetch the user's active key prefix, and handle regeneration with optimistic UI. Both hooks use the browser Supabase client. Verify: Use `useAuth()` in a client component — returns the current user. Use `useApiKey()` — returns the key prefix and regeneration function.

---

## Phase 4: Landing Page & Playground

> **Goal:** A polished marketing landing page with a live interactive playground that demonstrates MockHero's value without requiring signup. Visitors can paste a schema, select a locale, and see realistic relational data generated in real time. Includes features section, code examples, pricing comparison, and strong CTAs.

**Reference sections — read these before starting this phase:**
- PRD: § Functional Requirements > FR-016, FR-017, § UI/UX Requirements > Screen: Landing Page, § Design System (all sections)
- Vision: § Design Direction (all subsections), § Brand Voice & Messaging > Key Messages, § Brand Voice & Messaging > CTA Copy
- GTM: § Launch Strategy, § Content Strategy

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 4. Then read only the Reference sections listed above from docs/prd.md, docs/product-vision.md, and docs/gtm.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-4/landing-page-and-playground`, commit, push, and open a PR for review."

- [ ] **TASK-040** — Build the Hero section
  Files: `src/components/landing/hero.tsx`
  Notes: Full-width hero with headline, subline, and two CTAs. Headline: "Send a schema, get back realistic data." (or similar from docs/product-vision.md § Brand Voice & Messaging > Key Messages). Subline: one sentence about replacing seed scripts and Faker with one API call. Primary CTA: "Get Free API Key" linking to /signup. Secondary CTA: "Try the Playground" scrolling to the playground section. Background: subtle gradient or the `surface` color. Text hierarchy per typography tokens — hero text uses font-weight 700, text-5xl on desktop, text-3xl on mobile. Max width `--max-w-content` (1200px) centered. Per FR-016 and the brand voice (direct, confident, slightly witty). Verify: Hero section renders with correct typography, CTAs link correctly, responsive on mobile.

- [ ] **TASK-041** — Build the Live Playground section
  Files: `src/components/landing/playground.tsx`, `src/app/api/playground/route.ts`
  Notes: Two-panel layout: left panel is a code editor (use a `<textarea>` with `font-mono` styling — avoid heavy editors like Monaco for bundle size), right panel shows formatted output. Pre-populate with a compelling 2-table schema (users + orders with ref) per FR-017. Include a locale dropdown (en/de/fr/es/hr), format selector (JSON/SQL/CSV), and a "Generate" button. The playground hits a dedicated server-side route `POST /api/playground` that runs the generation engine without API key auth but with IP-based rate limiting (10 requests/hour per IP using a simple in-memory map with `headers().get('x-forwarded-for')`). Show generation time in the output metadata. Error state: if API fails, show static example output with note "Live preview temporarily unavailable." Per FR-017, US-012. Verify: Click "Generate" on landing page — output appears in <1 second with realistic relational data. Change locale to "de" — output shows German names. Rate limit kicks in after 10 requests.

- [ ] **TASK-042** — Build the Features section
  Files: `src/components/landing/features.tsx`
  Notes: Grid of 4 feature cards (2x2 on desktop, stacked on mobile). Cards: (1) "Locale-Aware Data" — German names for German users, not just John Smith, (2) "Relational Data" — foreign keys that actually work, generate an entire database in one call, (3) "Multiple Formats" — JSON, CSV, or SQL INSERT statements for Postgres, MySQL, SQLite, (4) "Agent-Ready" — MCP server for Cursor, Claude Code, and other AI coding agents. Each card: icon (use emoji or simple SVG for MVP), title (font-weight 600), description (text-muted, 2–3 sentences). Card style per design system. Per FR-016. Verify: Features section renders all 4 cards with correct content, responsive grid layout works.

- [ ] **TASK-043** — Build the Code Examples section
  Files: `src/components/landing/code-examples.tsx`, `src/components/ui/code-block.tsx`, `src/components/ui/tabs.tsx`
  Notes: Tabbed code examples showing cURL, JavaScript (fetch), and Python (requests) calls to the MockHero API. Use the Tabs component to switch between languages. Code blocks use JetBrains Mono font with syntax-highlighted-looking styling (use `bg-[#1A1A2E]` dark background with light text for all code blocks). Include a "Copy" button on each code block that copies to clipboard with toast. The examples should show a realistic 2-table generate request matching the playground schema. Per FR-016. Verify: All three tabs render correctly, copy button works, code blocks are styled with mono font on dark background.

- [ ] **TASK-044** — Build the Pricing section
  Files: `src/components/landing/pricing.tsx`
  Notes: Three-column pricing comparison (stacked on mobile). Free: $0/mo — 1,000 records/day, 100 per request, 5 locales, all formats, "Get Free API Key" button. Pro: $19/mo — 100,000 records/day, 10,000 per request, everything in Free + priority support, "Start Pro Trial" button (links to Polar checkout, disabled until Phase 5 — show "Coming Soon" for now). Scale: $49/mo — 1,000,000 records/day, 50,000 per request, everything in Pro + dedicated support, "Contact Us" or "Start Scale" button. Highlight Pro as "Most Popular" with a badge. Per US-013 and PRD § Data Model tier limits. Verify: Pricing section renders all 3 tiers with correct limits, "Most Popular" badge on Pro, responsive layout.

- [ ] **TASK-045** — Build the Footer
  Files: `src/components/landing/footer.tsx`
  Notes: Simple footer with MockHero logo/name, links (GitHub, Docs/API Reference — can link to /api/v1/types for now, Pricing — scrolls to pricing section, Login), and a copyright line. Subtle `surface` background. Per FR-016. Verify: Footer renders with all links, GitHub link opens in new tab.

- [ ] **TASK-046** — Assemble the landing page and add navigation header
  Files: `src/app/page.tsx`
  Notes: Compose the landing page from all sections: fixed navigation header (logo + nav links: Features, Playground, Pricing, Login/Dashboard) → Hero → Playground → Features → Code Examples → Pricing → Footer. The nav header should be transparent/blurred glass effect on scroll. Nav links use smooth scroll to section anchors. Show "Dashboard" link instead of "Login" if the user has an active session (check server-side). Responsive: hamburger menu on mobile. Per FR-016. Verify: Landing page loads in <2s (check with Lighthouse), all sections visible, smooth scroll works, nav links are correct, mobile hamburger works.

---

## Phase 5: Polish & Launch Prep

> **Goal:** Production-ready product with Polar payments integrated, MCP server built and publishable, comprehensive error handling, SEO optimization, analytics, and final testing. After this phase, MockHero is ready for public launch.

**Reference sections — read these before starting this phase:**
- PRD: § Functional Requirements > FR-020 (MCP Server), § Non-Functional Requirements (all), § Security Considerations (all), § API Specification > POST /api/v1/schema/detect (reference only — this is P2)
- Vision: § Design Direction > Accessibility Commitments, § Product Strategy > Post-MVP Roadmap
- GTM: § Launch Strategy, § Launch Week Plan, § Key Metrics

**Phase prompt — give this to your coding agent:**
> "Read docs/product-roadmap.md and find Phase 5. Then read only the Reference sections listed above from docs/prd.md, docs/product-vision.md, and docs/gtm.md. Continue from the first unchecked task. After each task, mark it complete in the roadmap. When all tasks are done, create a branch `phase-5/polish-and-launch-prep`, commit, push, and open a PR for review."

- [ ] **TASK-047** — Integrate Polar payments for Pro and Scale tiers
  Files: `src/lib/polar/client.ts`, `src/lib/polar/webhooks.ts`, `src/app/api/webhooks/polar/route.ts`, `package.json`
  Notes: Install `@polar-sh/sdk`. Create Polar client using `POLAR_API_KEY`. Build webhook handler at `/api/webhooks/polar` that verifies the webhook signature using `POLAR_WEBHOOK_SECRET`, then handles events: `subscription.created` → insert into `subscriptions` table and update `profiles.tier`, `subscription.updated` → update subscription status and tier, `subscription.canceled` → update status to 'canceled', update tier to 'free' at period end. The webhook handler must be idempotent. Add the "Upgrade to Pro" button in dashboard to redirect to Polar checkout URL with the user's email pre-filled. Per FR-020 in vision.json § business. See also docs/prd.md § Technical Architecture > Stack Integration Guide for Polar setup. Verify: Simulate a webhook event (use Polar's test mode) — subscription created in DB, user tier updated to 'pro'. Dashboard "Upgrade" button redirects to Polar checkout.

- [ ] **TASK-048** — Build the MCP server package
  Files: `mcp-server/package.json`, `mcp-server/tsconfig.json`, `mcp-server/src/index.ts`, `mcp-server/README.md`
  Notes: Create a standalone npm package in the `mcp-server/` directory. Package name: `@mockhero/mcp-server`. The server exposes two MCP tools: `generate_test_data` (accepts a schema object, calls POST /api/v1/generate with the user's API key from `MOCKHERO_API_KEY` env var, returns JSON data) and `list_field_types` (calls GET /api/v1/types, returns the types catalog). Use the `@modelcontextprotocol/sdk` package. Configure as a stdio transport MCP server. The README includes installation instructions, Claude Code config (`claude_desktop_config.json` format), and Cursor MCP config. Per FR-020, US-010, US-011. Verify: Build the package with `npm run build`. Configure it in Claude Code's MCP settings. Call `list_field_types` — returns the types catalog. Call `generate_test_data` with a simple schema — returns generated data.

- [ ] **TASK-049** — Add comprehensive error handling and edge cases
  Files: `src/app/api/v1/generate/route.ts`, `src/lib/engine/generator.ts`, `src/lib/engine/schema-parser.ts`
  Notes: Handle all edge cases from PRD § API Specification: count of 0 returns empty array, circular dependencies return DEPENDENCY_CYCLE error with cycle path, request body >1MB returns 413, field count >50 per table returns 400, table count >20 returns 400. Add try/catch wrappers around the generation engine — if it throws, return 500 with `INTERNAL_ERROR`. Add timeout protection: if generation takes >25 seconds, abort and return 500 with "Generation timed out. Try reducing record count." Add graceful Supabase error handling: if DB is unreachable during auth check, return 503. Per PRD § Non-Functional Requirements > Reliability. Verify: Send a schema with circular deps — get DEPENDENCY_CYCLE error. Send 21 tables — get validation error. Send count: 0 — get empty array in response.

- [ ] **TASK-050** — Add SEO metadata, Open Graph tags, and sitemap
  Files: `src/app/layout.tsx`, `src/app/sitemap.ts`, `src/app/robots.ts`, `public/og-image.png`
  Notes: Update root layout metadata: title template `%s | MockHero`, default title "MockHero — Realistic Test Data API", description "Send a schema, get back realistic, relational fake data. One API call replaces hours of seed scripting." Add OG tags: og:image (create a simple 1200x630 OG image — can be a branded solid color with text for MVP), og:type "website", og:url. Add Twitter card meta tags (summary_large_image). Create `sitemap.ts` that exports the sitemap function returning URLs for `/`, `/login`, `/signup`. Create `robots.ts` allowing all crawlers, pointing to sitemap. Verify: View page source — OG tags present. Run Lighthouse SEO audit — score above 90.

- [ ] **TASK-051** — Add analytics tracking
  Files: `src/app/layout.tsx`, `src/components/analytics.tsx`
  Notes: Add Vercel Analytics (`@vercel/analytics`) and Vercel Speed Insights (`@vercel/speed-insights`) — both are free. Create an `Analytics` client component that renders `<Analytics />` and `<SpeedInsights />` from the respective packages. Include in root layout. These track page views, web vitals, and custom events automatically. Per GTM § Key Metrics — track core web vitals and page views. Verify: `npm run build` succeeds with analytics packages. Deploy to Vercel — analytics events appear in Vercel dashboard.

- [ ] **TASK-052** — Accessibility pass
  Files: Multiple component files
  Notes: Audit all interactive elements for WCAG 2.1 AA compliance per PRD § Non-Functional Requirements > Accessibility. Ensure: all buttons and links are keyboard accessible with visible focus indicators (`ring-2 ring-primary/20`), form inputs have associated `<label>` elements, toast notifications use `aria-live="polite"`, color contrast minimum 4.5:1 for text (verify primary purple #6C5CE7 on white meets contrast ratio — it does at 4.6:1 for large text; for small text, use on `--color-primary-light` background or darkened text), touch targets minimum 44x44px on mobile, `prefers-reduced-motion` disables all animations. Verify: Tab through entire app — all interactive elements are reachable. Run Lighthouse accessibility audit — score above 90.

- [ ] **TASK-053** — Security hardening
  Files: `src/middleware.ts`, `next.config.ts`
  Notes: Add security headers in `next.config.ts`: `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy` (disable camera, microphone, geolocation). Add basic CSP header: `default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://va.vercel-scripts.com; style-src 'self' 'unsafe-inline'; img-src 'self' https: data:; font-src 'self' https://fonts.gstatic.com; connect-src 'self' https://*.supabase.co`. Configure CORS for API routes: allow all origins for `/api/v1/*` endpoints (API keys provide auth), restrict dashboard API routes to the app domain. Per PRD § Non-Functional Requirements > Security. Verify: Check response headers with `curl -I` — security headers present. API routes are accessible from any origin. Dashboard routes reject cross-origin requests.

- [ ] **TASK-054** — Responsive design pass
  Files: Multiple component files
  Notes: Test and fix responsive layout across three breakpoints: mobile (375px), tablet (768px), desktop (1280px). Key areas: landing page hero stacks vertically on mobile, playground becomes single-column on mobile with editor above output, features grid becomes single column on mobile, pricing cards stack on mobile, dashboard cards stack on mobile, navigation collapses to hamburger on mobile. Ensure no horizontal scroll on any viewport. All touch targets minimum 44x44px on mobile. Verify: Resize browser to 375px width — no horizontal scroll, all sections readable, navigation works via hamburger menu.

- [ ] **TASK-055** — Performance optimization
  Files: `next.config.ts`, various component files
  Notes: Optimize for PRD § Non-Functional Requirements > Performance targets. Landing page LCP <2.0s: ensure hero text is SSR'd (not client-rendered), lazy-load playground component with `next/dynamic`, optimize images with `next/image`. Bundle size <200KB gzipped: check with `npm run build` output, tree-shake unused code. API response time <200ms for 100 records: the engine is already pure TS and fast, but verify with timing logs. Dashboard load <2.0s: lazy-load the usage chart. Add `loading.tsx` files for dashboard route segments. Verify: Run Lighthouse on landing page — LCP <2.0s, TTI <3.0s. Check `npm run build` output for bundle sizes.

- [ ] **TASK-056** — Add loading and empty states across the app
  Files: `src/app/dashboard/loading.tsx`, `src/app/(auth)/login/loading.tsx`
  Notes: Dashboard loading state: skeleton shimmer for API key card, usage chart, and tier info (use the Skeleton component). Auth pages loading state: centered spinner. Landing page playground loading state: skeleton for code editor and output blocks. Empty state for usage chart when no data: "No API calls yet. Use the cURL example above to make your first request." Error states: dashboard shows "Couldn't load usage data. Refresh to try again." if Supabase query fails, playground shows static example if API fails. Verify: Throttle network to Slow 3G — loading states appear before content. New user dashboard shows empty state correctly.

- [ ] **TASK-057** — Configure Vercel deployment and environment variables
  Files: `vercel.json` (if needed), `.github/workflows/ci.yml` (optional)
  Notes: Ensure `next.config.ts` has correct settings: increase serverless function timeout to 30s for `/api/v1/generate` route (use `maxDuration` in route segment config). Set up Vercel project environment variables (document the required vars in README or .env.example). Optionally create a basic GitHub Actions CI workflow that runs `tsc --noEmit`, `eslint .`, and `vitest run` on every PR. Per PRD § Infrastructure & Deployment > CI/CD. Verify: Push to main — Vercel deploys successfully. All API routes work in production. Environment variables are correctly resolved.

- [ ] **TASK-058** — Add early adopter hook and signup flow enhancement
  Files: `src/lib/utils/constants.ts`, `src/app/(auth)/signup/page.tsx`
  Notes: Per GTM § Launch Strategy — offer 10,000 free records/day for the first 100 signups (10x normal free tier). Add an `EARLY_ADOPTER_LIMIT = 100` constant and an `EARLY_ADOPTER_DAILY_RECORDS = 10000` value. On signup, check the total user count in `profiles` table. If under 100, set the user's profile with a flag or use a special 'early_adopter' designation. Update the rate limiter to check for early adopter status and use the higher limit. Add a badge on the dashboard for early adopters: "Early Adopter — 10x Free Tier." On the signup page, show a counter: "X of 100 early adopter spots remaining." Verify: Sign up as one of the first 100 users — daily limit is 10,000 records. Dashboard shows early adopter badge.

- [ ] **TASK-059** — Write the OpenAPI/types documentation page
  Files: `src/app/docs/page.tsx` (optional — can redirect to /api/v1/types)
  Notes: Create a minimal documentation route at `/docs` that either renders the output of GET /api/v1/types in a nicely formatted page (categorized field types with descriptions, params, and examples) or simply redirects to `/api/v1/types` with a note that this is the canonical reference. For MVP, a simple redirect or formatted rendering of the types endpoint is sufficient — full docs can come post-launch. Include a link to the API documentation from the landing page footer and dashboard. Verify: `/docs` is accessible and shows all supported field types with descriptions and examples.

- [ ] **TASK-060** — End-to-end smoke test
  Files: None (manual testing)
  Notes: Walk through the complete user journey end-to-end: (1) Visit landing page — all sections render, playground works. (2) Click "Get Free API Key" → sign up with email or GitHub. (3) Redirected to dashboard — API key shown in full, copy it. (4) Use cURL to call /api/v1/generate with the API key — get back realistic data. (5) Try a multi-table schema with refs — foreign keys are correct (the magic moment). (6) Try all 3 formats (JSON, CSV, SQL). (7) Try all 5 locales. (8) Check usage stats on dashboard — today's usage updates. (9) Regenerate API key — old key returns 401, new key works. (10) Visit /api/v1/types — full types catalog returned. (11) Visit /api/v1/health — returns ok. Verify: All 11 steps complete without errors. The magic moment works: 4-table relational schema returns correct data in <1 second.

- [ ] **TASK-061** — Final pre-launch checklist
  Files: `package.json`, `next.config.ts`
  Notes: Run through this checklist: (1) `npm run build` — no errors or warnings. (2) `npx vitest run` — all tests pass. (3) Lighthouse score: Performance >90, Accessibility >90, SEO >90. (4) All environment variables set in Vercel. (5) Supabase migration applied to production project. (6) Polar products created (Pro $19/mo, Scale $49/mo) with correct webhook URL. (7) OG image renders correctly when shared on Twitter/X (test with card validator). (8) Custom domain configured (mockhero.dev if purchased). (9) Error monitoring: Vercel logs show no unhandled errors. (10) Rate limiting works: free tier capped at 1,000 records/day. Verify: All 10 items checked. The product is ready for the GTM launch plan in docs/gtm.md § Launch Week Plan.

- [ ] **TASK-062** — Prepare launch assets
  Files: None (content creation)
  Notes: Prepare the content needed for launch per GTM § Launch Week Plan: (1) Draft the "Show HN" title and description: "Show HN: MockHero — Realistic, relational test data from a schema definition" with 3-sentence description (what it does, why, differentiator). (2) Draft the first Twitter/X launch post announcing MockHero with the early adopter offer. (3) Prepare 3 responses to likely HN comments: "How is this different from Faker?", "What about Mockaroo?", "Why would I pay for this?" (4) Take a screenshot of the playground showing German relational data for social sharing. (5) Test the MCP server package — ensure it's ready to publish to npm. Save all drafts in a `launch-assets/` folder or notes file. Verify: All 5 launch assets are prepared and reviewed.

---

## Agent Session Guide

### How to Use This Roadmap with Your Coding Agent

1. **Start a session:** Give your coding agent the phase prompt at the beginning of each phase.
2. **Read selectively:** Each phase lists its Reference sections — the specific parts of the PRD, vision doc, and GTM doc needed for that phase. The agent should read only those sections, not the entire documents.
3. **Let it work:** The agent reads the roadmap, finds the first unchecked task, implements it, and marks it complete.
4. **One session = one phase (ideally):** Try to complete a full phase in one session for best continuity. If you need to stop, the agent can resume from the last unchecked task.
5. **Push a PR for review:** When a phase is complete, push the work as a PR and let a review agent (e.g. [CodeRabbit](https://coderabbit.ai)) review it before starting the next phase. See the Phase Review section below.
6. **Need more context?** If a task references a section not in the phase's Reference sections, the agent should read just that section on demand.

### Phase Review Workflow

After completing each phase, the coding agent pushes the work as a pull request for external review. This is a quality gate — the next phase should not start until the PR is reviewed and merged.

**Branch naming:** `phase-{N}/{phase-slug}` — e.g., `phase-0/foundation-and-setup`, `phase-1/data-generation-engine`.

**PR format:**
```
Title: Phase {N}: {Phase Title}

Body:
## Goal
{The phase's goal statement from the roadmap}

## Completed
- {X} tasks completed (TASK-{first} through TASK-{last})

## What to verify
- {Key things to test manually — derived from task verification notes}

## Reference sections used
- PRD: § {sections}
- Vision: § {sections}
- GTM: § {sections} (if applicable)
```

**Review agent:** Use [CodeRabbit](https://coderabbit.ai) (free for open-source) or another automated review tool. If the review flags issues, address them in follow-up commits on the same branch before merging.

### Session Tips

- **Don't read everything:** The PRD, vision doc, and GTM doc are large. Each phase's Reference sections tell the agent exactly what to read. Loading the full documents wastes context.
- **Don't skip tasks:** Tasks are ordered intentionally. Skipping creates dependency issues.
- **Verify after each phase:** Run the app after completing a phase to confirm everything works before moving on.
- **Review before moving on:** Push a PR for each completed phase and let your review agent check it. Don't start the next phase until the PR is merged. This catches issues early when they're cheap to fix.
- **Update the status line:** After completing tasks, update the header status: `**Status:** X/62 tasks complete` and `**Current Phase:** Phase N`.

### Prompt Templates

**Starting a new phase:**
> "Read docs/product-roadmap.md and find the current phase. Read only the Reference sections listed for that phase from docs/prd.md, docs/product-vision.md, and docs/gtm.md. Start working on the first unchecked task. After completing each task, update the checkbox to [x] in the roadmap file. Continue through the phase."

**Resuming after a break:**
> "Read docs/product-roadmap.md. Find where we left off (first unchecked task). Read only the Reference sections listed for the current phase from docs/prd.md, docs/product-vision.md, and docs/gtm.md. Continue from the first unchecked task."

**After completing a phase:**
> "Phase [N] is complete. Create a branch called phase-{N}/{slug}, commit all work, push, and open a PR targeting main. Title it 'Phase {N}: {Title}' and include the phase goal and completed task count in the body."

**Fixing an issue:**
> "There's a problem with [description]. Read the relevant section of docs/prd.md for the expected behavior and fix it. Don't mark any new tasks complete until the fix is verified."
